# ── Production Docker requirements ──────────────────────────────────────────
# Pinned to EXACTLY match the versions the .pkl models were trained with.
# Models were saved with scikit-learn==1.7.2 and xgboost==3.2.0 — do not change.
flask>=3.0.0
flask-cors>=4.0.0
joblib>=1.3.0
numpy==2.0.2
pandas>=2.0.0
scikit-learn==1.7.2
xgboost==3.2.0
python-dotenv>=1.0.0
gunicorn>=21.0.0
pymongo>=4.6.0
bcrypt>=4.0.0
PyJWT>=2.8.0
google-auth>=2.25.0
requests>=2.31.0
Pillow>=10.0.0
